{% extends 'base.html.twig' %}

{% block title %}
    Ajouter une entreprise
{% endblock %}

{% block body %}
    <!DOCTYPE html>
    <html>
        <head>
            <title>
                Ajout
            </title>
        </head>
        <body>
            <div class="main">
                <div class="header">
                    <div class="titrePage">
                        <h2>
                            Ajouter une entreprise
                        </h2>
                    </div>
                </div>

                <div class="container">
                    <h1> Veuillez saisir les informations de l'entreprise que vous souhaitez ajouter </h1>
                    {{ form_start(vueFormulaire, {'attr':{'novalidate': 'novalidate '}}) }}
                        {{ form_row(vueFormulaire.activite) }}
                        {{ form_row(vueFormulaire.nom) }}
                        {{ form_row(vueFormulaire.activite) }}
                        {{ form_row(vueFormulaire.adresse) }}
                        {{ form_row(vueFormulaire.URLSite) }}
                        <br>
                        <button type="submit" class="btn btn-dark">Envoyer</button>
                        
                    {{ form_end(vueFormulaire) }}
                </div>
            </div>
        </body>
    </html>
</div></body></html></body></html></div></body></html>{% endblock %}